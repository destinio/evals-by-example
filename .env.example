# Copy to .env, or just run `bun run setup`.

# Model access. Any OpenAI-compatible endpoint works.
NOUS_API_KEY=
# NOUS_BASE_URL=https://inference-api.nousresearch.com/v1
# MODEL=anthropic/claude-haiku-4.5

# Braintrust — free tier is plenty. Needed from step 1 onward, not to run the app.
BRAINTRUST_API_KEY=

# Step N — Title

**The idea:** one sentence. What this step is actually for.

> Working with Claude Code? Run `/tutor` and it'll walk this step with you, then check it.

```bash
git checkout -b step-N-slug
```

## Why this now

The problem in the app's own terms, not in eval jargon. What can't be answered or trusted today, and what it costs to leave it that way. Keep it short — a paragraph and a few concrete questions the current setup can't answer.

## What you'll add

The pieces, and why each one earns its place. If two things are being introduced, say what each is responsible for and what breaks if you skip one.

## Do it

Numbered actions. Code blocks in the repo's style — single quotes, no semicolons. Small enough to read in one screen; if a block is longer than that, split it and say what each half does.

## Check yourself

Where to look, and what should be there. Name the specific rows, numbers or screens — "four traces named `daily report`, each with a nested child" beats "you should see your traces". Include what it looks like when it went wrong, if there's an obvious failure mode.

## Commit

```bash
git add -A && git commit -m "step N: what changed"
```

## What you can do now

The capability gained, in one or two sentences — this is the demo beat.

**What you still can't do:** the gap that motivates the next step.
